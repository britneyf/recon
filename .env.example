# Copy this file to `.env` and paste your key. The guardian CLI loads it automatically.
#   cp .env.example .env
# A real exported ANTHROPIC_API_KEY in your shell always wins over this file.
ANTHROPIC_API_KEY=sk-ant-your-key-here
